import 'package:flutter/material.dart';

class SummaryNewsPostScreen extends StatelessWidget {
  const SummaryNewsPostScreen({Key? key, this.groupId}) : super(key: key);
  final int? groupId;
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Tính năng đang phát triển'),
    );
  }
}
